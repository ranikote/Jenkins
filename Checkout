pipeline {
    agent any

    stages {
        stage('git Checkout') {
            steps {
            
                git 'https://github.com/ranikote/addressbook.git'
            }
        }
    }
}
